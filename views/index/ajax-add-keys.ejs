<% for(var i=0; i < keys.length; i++){ %>
<label class="checkbox-inline">
    <input type="checkbox" name="key" id="<%= cate %>-key" value="<%= keys[i].keyname %>">
    <span class="badge"><%= keys[i].keyname %></span>
</label>
<% } %>